# Canonical setup is now the repo-root uv workspace: run `uv sync` at the
# repo root, then `uv run trading-dashboard/app.py`. This file is kept only
# for standalone (non-uv) installs and may lag the workspace pins.
Flask==3.1.3
flask-cors==6.0.0
yfinance==0.2.54
pandas>=2.2.0
requests>=2.32.4
schwab-py>=1.5.0
