[project]
name = "trading-dashboard"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "stocks-shared",
    "flask",
    "flask-cors",
    "schwab-py",
    "yfinance",
    "requests",
]

[dependency-groups]
dev = ["pytest>=8"]
